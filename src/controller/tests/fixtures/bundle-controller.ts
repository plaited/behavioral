/**
 * Re-export of the production bundler — the fixture server and the instance
 * socket host share one bundle home (no forked copies).
 */
export { bundleController, CONNECT_BEHAVIORAL_ROUTE } from '../../bundle-controller.ts'
